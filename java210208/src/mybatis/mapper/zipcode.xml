<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mybatis.mapper.ZipcodeMapper">
	<select id="getZDOList" resultType="String">

		SELECT '전체' zdo FROM dual
		UNION ALL
		SELECT zdo FROM
		(SELECT DISTINCT(zdo) zdo FROM zipcode_t ORDER
		BY zdo)

	</select>
	<select id="refreshData" parameterType="java.util.Map" resultType="java.util.Map">

		SELECT address, zipcode FROM zipcode_t WHERE 1 = 1

		<if test="zdo != null and zdo.length() >">
			AND zdo = #{zdo}
		</if>
		<if test="sigu != null and zdo.length() >">
			AND sigu LIKE '%'||#{sigu}||'%'
		</if>
		<if test="dong != null and zdo.length() >">
			AND dong LIKE '%'||#{dong}||'%'
		</if>
	</select>
	<select id="refreshData2" parameterType="com.design.zipcode.ZipCodeVO" resultType="com.design.zipcode.ZipCodeVO">

		SELECT address, zipcode FROM zipcode_t WHERE 1 = 1

		<if test="zdo != null and zdo.length() >">
			AND zdo = #{zdo}
		</if>
		<if test="sigu != null and zdo.length() >">
			AND sigu LIKE '%'||#{sigu}||'%'
		</if>
		<if test="dong != null and zdo.length() >">
			AND dong LIKE '%'||#{dong}||'%'
		</if>
	</select>
</mapper>
